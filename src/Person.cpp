#include "Person.h"

int Person::getIndex() const {
    return index;
}

std::string Person::getFirstname() const {
    return firstname;
}

std::string Person::getLastname() const {
    return lastname;
}